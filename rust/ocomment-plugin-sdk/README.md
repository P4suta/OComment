# ocomment-plugin-sdk

Host- and guest-side validation types for OComment scanner plugins. Plugins
return only sorted, non-overlapping comment spans and kinds; the host owns policy
classification and every file edit.

The versioned WIT contract and plugin guide live in the
[OComment repository](https://github.com/P4suta/OComment).
