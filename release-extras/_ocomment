#compdef ocomment

autoload -U is-at-least

_ocomment() {
    typeset -A opt_args
    typeset -a _arguments_options
    local ret=1

    if is-at-least 5.2; then
        _arguments_options=(-s -S -C)
    else
        _arguments_options=(-s -C)
    fi

    local context curcontext="$curcontext" state line
    _arguments "${_arguments_options[@]}" : \
'--config=[Read this configuration file instead of discovering \`.ocomment.toml\`]:FILE:_files' \
'--policy=[Which classes of comment the run is allowed to remove]:POLICY:((safe\:"Remove ordinary and doc comments; keep preambles and directives"
legal\:"Like safe, and keep licence and copyright comments as well"
all\:"Remove every comment that no keep override protects"))' \
'--layout=[How the bytes left behind by a removed comment are laid out]:LAYOUT:((lines\:"Keep the line structure and separate tokens that would otherwise join"
columns\:"Pad each removed comment so the following columns do not shift"
compact\:"Drop lines that held only a removed comment, and the whitespace it left behind"))' \
'--language=[Force this language instead of detecting it from path and contents]:LANGUAGE:((rust\:"Rust source files"
ocaml\:"OCaml implementation and interface files"
c\:"C source and header files"
cpp\:"C++ source and header files"
go\:"Go source files"
java\:"Java source files, including Unicode escape translation"
javascript\:"JavaScript modules and scripts, including JSX"
typescript\:"TypeScript modules and scripts, including TSX"
python\:"Python source and stub files"
shell\:"POSIX sh, Bash, and zsh scripts"
html\:"HTML documents, including nested script and style elements"
css\:"CSS stylesheets"
jsonc\:"JSON with comments, including JSON5"
sql\:"SQL for every supported database dialect"
kotlin\:"Kotlin source and script files"
toml\:"TOML documents, including the lock files written in it"
lua\:"Lua chunks and LuaRocks rockspecs"
yaml\:"YAML documents, including the tool configurations written in it"
php\:"PHP scripts and templates; the inline HTML around the tags is content"
ruby\:"Ruby scripts, gem manifests, and the project files named after their tool"))' \
'--dialect=[Force this dialect of the selected language]:DIALECT:((standard\:"The default lexical rules of the language"
jsx\:"JavaScript with JSX elements"
tsx\:"TypeScript with JSX elements"
objective-c\:"Objective-C extensions to C"
objective-cpp\:"Objective-C++ extensions to C++"
gnu-c\:"GNU extensions to C"
gnu-cpp\:"GNU extensions to C++"
cuda\:"CUDA extensions to C++"
posix-sh\:"The POSIX shell command language"
bash53\:"Bash 5.3"
zsh\:"The Z shell"
postgresql\:"PostgreSQL, with dollar-quoted bodies"
mysql\:"MySQL, including its executable versioned comments"
sqlite\:"SQLite"
t-sql\:"Microsoft Transact-SQL"
oracle\:"Oracle SQL and PL/SQL"))' \
'*--keep-kind=[Comma-separated comment kinds to protect on top of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'*--remove-kind=[Comma-separated comment kinds to remove regardless of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'--format=[Output encoding]:FORMAT:(human json jsonl sarif github)' \
'--color=[When to colour terminal output]:WHEN:(auto always never)' \
'--hyperlinks=[When to emit terminal hyperlinks for reported paths]:WHEN:(auto always never)' \
'--progress=[When to draw the live scanning counter on standard error]:WHEN:(auto always never)' \
'--force-invalid[Apply the edits that are still provably safe when the source fails to scan]' \
'--force-protected[Remove protected comments such as shebang and encoding preambles]' \
'--no-preview[Omit the one-line comment text from human \`check\` and \`scan\` lines]' \
'--explain[List every comment human \`check\` and \`scan\` met and name the rule and setting behind each one]' \
'(-v --verbose)-q[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'(-v --verbose)--quiet[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'-v[Trace what is scanned and summarize every comment kind and skipped file]' \
'--verbose[Trace what is scanned and summarize every comment kind and skipped file]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'-V[Print version]' \
'--version[Print version]' \
'::paths -- Files or directories to check; `-` reads standard input (default\: current directory):_files' \
":: :_ocomment_commands" \
"*::: :->ocomment" \
&& ret=0
    case $state in
    (ocomment)
        words=($line[2] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:ocomment-command-$line[2]:"
        case $line[2] in
            (check)
_arguments "${_arguments_options[@]}" : \
'--config=[Read this configuration file instead of discovering \`.ocomment.toml\`]:FILE:_files' \
'--policy=[Which classes of comment the run is allowed to remove]:POLICY:((safe\:"Remove ordinary and doc comments; keep preambles and directives"
legal\:"Like safe, and keep licence and copyright comments as well"
all\:"Remove every comment that no keep override protects"))' \
'--layout=[How the bytes left behind by a removed comment are laid out]:LAYOUT:((lines\:"Keep the line structure and separate tokens that would otherwise join"
columns\:"Pad each removed comment so the following columns do not shift"
compact\:"Drop lines that held only a removed comment, and the whitespace it left behind"))' \
'--language=[Force this language instead of detecting it from path and contents]:LANGUAGE:((rust\:"Rust source files"
ocaml\:"OCaml implementation and interface files"
c\:"C source and header files"
cpp\:"C++ source and header files"
go\:"Go source files"
java\:"Java source files, including Unicode escape translation"
javascript\:"JavaScript modules and scripts, including JSX"
typescript\:"TypeScript modules and scripts, including TSX"
python\:"Python source and stub files"
shell\:"POSIX sh, Bash, and zsh scripts"
html\:"HTML documents, including nested script and style elements"
css\:"CSS stylesheets"
jsonc\:"JSON with comments, including JSON5"
sql\:"SQL for every supported database dialect"
kotlin\:"Kotlin source and script files"
toml\:"TOML documents, including the lock files written in it"
lua\:"Lua chunks and LuaRocks rockspecs"
yaml\:"YAML documents, including the tool configurations written in it"
php\:"PHP scripts and templates; the inline HTML around the tags is content"
ruby\:"Ruby scripts, gem manifests, and the project files named after their tool"))' \
'--dialect=[Force this dialect of the selected language]:DIALECT:((standard\:"The default lexical rules of the language"
jsx\:"JavaScript with JSX elements"
tsx\:"TypeScript with JSX elements"
objective-c\:"Objective-C extensions to C"
objective-cpp\:"Objective-C++ extensions to C++"
gnu-c\:"GNU extensions to C"
gnu-cpp\:"GNU extensions to C++"
cuda\:"CUDA extensions to C++"
posix-sh\:"The POSIX shell command language"
bash53\:"Bash 5.3"
zsh\:"The Z shell"
postgresql\:"PostgreSQL, with dollar-quoted bodies"
mysql\:"MySQL, including its executable versioned comments"
sqlite\:"SQLite"
t-sql\:"Microsoft Transact-SQL"
oracle\:"Oracle SQL and PL/SQL"))' \
'*--keep-kind=[Comma-separated comment kinds to protect on top of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'*--remove-kind=[Comma-separated comment kinds to remove regardless of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'--format=[Output encoding]:FORMAT:(human json jsonl sarif github)' \
'--color=[When to colour terminal output]:WHEN:(auto always never)' \
'--hyperlinks=[When to emit terminal hyperlinks for reported paths]:WHEN:(auto always never)' \
'--progress=[When to draw the live scanning counter on standard error]:WHEN:(auto always never)' \
'--staged[Read and update Git index blobs rather than treating the working tree as the source]' \
'--index-only[With \`--staged\`, do not attempt a uniquely mappable working-tree update]' \
'--force-invalid[Apply the edits that are still provably safe when the source fails to scan]' \
'--force-protected[Remove protected comments such as shebang and encoding preambles]' \
'--no-preview[Omit the one-line comment text from human \`check\` and \`scan\` lines]' \
'--explain[List every comment human \`check\` and \`scan\` met and name the rule and setting behind each one]' \
'(-v --verbose)-q[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'(-v --verbose)--quiet[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'-v[Trace what is scanned and summarize every comment kind and skipped file]' \
'--verbose[Trace what is scanned and summarize every comment kind and skipped file]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'*::paths -- Files or directories to process; `-` reads standard input (default\: current directory):_files' \
&& ret=0
;;
(fix)
_arguments "${_arguments_options[@]}" : \
'--config=[Read this configuration file instead of discovering \`.ocomment.toml\`]:FILE:_files' \
'--policy=[Which classes of comment the run is allowed to remove]:POLICY:((safe\:"Remove ordinary and doc comments; keep preambles and directives"
legal\:"Like safe, and keep licence and copyright comments as well"
all\:"Remove every comment that no keep override protects"))' \
'--layout=[How the bytes left behind by a removed comment are laid out]:LAYOUT:((lines\:"Keep the line structure and separate tokens that would otherwise join"
columns\:"Pad each removed comment so the following columns do not shift"
compact\:"Drop lines that held only a removed comment, and the whitespace it left behind"))' \
'--language=[Force this language instead of detecting it from path and contents]:LANGUAGE:((rust\:"Rust source files"
ocaml\:"OCaml implementation and interface files"
c\:"C source and header files"
cpp\:"C++ source and header files"
go\:"Go source files"
java\:"Java source files, including Unicode escape translation"
javascript\:"JavaScript modules and scripts, including JSX"
typescript\:"TypeScript modules and scripts, including TSX"
python\:"Python source and stub files"
shell\:"POSIX sh, Bash, and zsh scripts"
html\:"HTML documents, including nested script and style elements"
css\:"CSS stylesheets"
jsonc\:"JSON with comments, including JSON5"
sql\:"SQL for every supported database dialect"
kotlin\:"Kotlin source and script files"
toml\:"TOML documents, including the lock files written in it"
lua\:"Lua chunks and LuaRocks rockspecs"
yaml\:"YAML documents, including the tool configurations written in it"
php\:"PHP scripts and templates; the inline HTML around the tags is content"
ruby\:"Ruby scripts, gem manifests, and the project files named after their tool"))' \
'--dialect=[Force this dialect of the selected language]:DIALECT:((standard\:"The default lexical rules of the language"
jsx\:"JavaScript with JSX elements"
tsx\:"TypeScript with JSX elements"
objective-c\:"Objective-C extensions to C"
objective-cpp\:"Objective-C++ extensions to C++"
gnu-c\:"GNU extensions to C"
gnu-cpp\:"GNU extensions to C++"
cuda\:"CUDA extensions to C++"
posix-sh\:"The POSIX shell command language"
bash53\:"Bash 5.3"
zsh\:"The Z shell"
postgresql\:"PostgreSQL, with dollar-quoted bodies"
mysql\:"MySQL, including its executable versioned comments"
sqlite\:"SQLite"
t-sql\:"Microsoft Transact-SQL"
oracle\:"Oracle SQL and PL/SQL"))' \
'*--keep-kind=[Comma-separated comment kinds to protect on top of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'*--remove-kind=[Comma-separated comment kinds to remove regardless of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'--format=[Output encoding]:FORMAT:(human json jsonl sarif github)' \
'--color=[When to colour terminal output]:WHEN:(auto always never)' \
'--hyperlinks=[When to emit terminal hyperlinks for reported paths]:WHEN:(auto always never)' \
'--progress=[When to draw the live scanning counter on standard error]:WHEN:(auto always never)' \
'--staged[Read and update Git index blobs rather than treating the working tree as the source]' \
'--index-only[With \`--staged\`, do not attempt a uniquely mappable working-tree update]' \
'--dry-run[Print the patch \`fix\` would apply and write nothing]' \
'(--staged --dry-run -q --quiet)-i[Ask about each comment in turn and remove only the accepted ones]' \
'(--staged --dry-run -q --quiet)--interactive[Ask about each comment in turn and remove only the accepted ones]' \
'--force-invalid[Apply the edits that are still provably safe when the source fails to scan]' \
'--force-protected[Remove protected comments such as shebang and encoding preambles]' \
'--no-preview[Omit the one-line comment text from human \`check\` and \`scan\` lines]' \
'--explain[List every comment human \`check\` and \`scan\` met and name the rule and setting behind each one]' \
'(-v --verbose)-q[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'(-v --verbose)--quiet[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'-v[Trace what is scanned and summarize every comment kind and skipped file]' \
'--verbose[Trace what is scanned and summarize every comment kind and skipped file]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'*::paths -- Files or directories to rewrite (default\: current directory):_files' \
&& ret=0
;;
(diff)
_arguments "${_arguments_options[@]}" : \
'--config=[Read this configuration file instead of discovering \`.ocomment.toml\`]:FILE:_files' \
'--policy=[Which classes of comment the run is allowed to remove]:POLICY:((safe\:"Remove ordinary and doc comments; keep preambles and directives"
legal\:"Like safe, and keep licence and copyright comments as well"
all\:"Remove every comment that no keep override protects"))' \
'--layout=[How the bytes left behind by a removed comment are laid out]:LAYOUT:((lines\:"Keep the line structure and separate tokens that would otherwise join"
columns\:"Pad each removed comment so the following columns do not shift"
compact\:"Drop lines that held only a removed comment, and the whitespace it left behind"))' \
'--language=[Force this language instead of detecting it from path and contents]:LANGUAGE:((rust\:"Rust source files"
ocaml\:"OCaml implementation and interface files"
c\:"C source and header files"
cpp\:"C++ source and header files"
go\:"Go source files"
java\:"Java source files, including Unicode escape translation"
javascript\:"JavaScript modules and scripts, including JSX"
typescript\:"TypeScript modules and scripts, including TSX"
python\:"Python source and stub files"
shell\:"POSIX sh, Bash, and zsh scripts"
html\:"HTML documents, including nested script and style elements"
css\:"CSS stylesheets"
jsonc\:"JSON with comments, including JSON5"
sql\:"SQL for every supported database dialect"
kotlin\:"Kotlin source and script files"
toml\:"TOML documents, including the lock files written in it"
lua\:"Lua chunks and LuaRocks rockspecs"
yaml\:"YAML documents, including the tool configurations written in it"
php\:"PHP scripts and templates; the inline HTML around the tags is content"
ruby\:"Ruby scripts, gem manifests, and the project files named after their tool"))' \
'--dialect=[Force this dialect of the selected language]:DIALECT:((standard\:"The default lexical rules of the language"
jsx\:"JavaScript with JSX elements"
tsx\:"TypeScript with JSX elements"
objective-c\:"Objective-C extensions to C"
objective-cpp\:"Objective-C++ extensions to C++"
gnu-c\:"GNU extensions to C"
gnu-cpp\:"GNU extensions to C++"
cuda\:"CUDA extensions to C++"
posix-sh\:"The POSIX shell command language"
bash53\:"Bash 5.3"
zsh\:"The Z shell"
postgresql\:"PostgreSQL, with dollar-quoted bodies"
mysql\:"MySQL, including its executable versioned comments"
sqlite\:"SQLite"
t-sql\:"Microsoft Transact-SQL"
oracle\:"Oracle SQL and PL/SQL"))' \
'*--keep-kind=[Comma-separated comment kinds to protect on top of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'*--remove-kind=[Comma-separated comment kinds to remove regardless of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'--format=[Output encoding]:FORMAT:(human json jsonl sarif github)' \
'--color=[When to colour terminal output]:WHEN:(auto always never)' \
'--hyperlinks=[When to emit terminal hyperlinks for reported paths]:WHEN:(auto always never)' \
'--progress=[When to draw the live scanning counter on standard error]:WHEN:(auto always never)' \
'--staged[Read and update Git index blobs rather than treating the working tree as the source]' \
'--index-only[With \`--staged\`, do not attempt a uniquely mappable working-tree update]' \
'--force-invalid[Apply the edits that are still provably safe when the source fails to scan]' \
'--force-protected[Remove protected comments such as shebang and encoding preambles]' \
'--no-preview[Omit the one-line comment text from human \`check\` and \`scan\` lines]' \
'--explain[List every comment human \`check\` and \`scan\` met and name the rule and setting behind each one]' \
'(-v --verbose)-q[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'(-v --verbose)--quiet[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'-v[Trace what is scanned and summarize every comment kind and skipped file]' \
'--verbose[Trace what is scanned and summarize every comment kind and skipped file]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'*::paths -- Files or directories to process; `-` reads standard input (default\: current directory):_files' \
&& ret=0
;;
(scan)
_arguments "${_arguments_options[@]}" : \
'--config=[Read this configuration file instead of discovering \`.ocomment.toml\`]:FILE:_files' \
'--policy=[Which classes of comment the run is allowed to remove]:POLICY:((safe\:"Remove ordinary and doc comments; keep preambles and directives"
legal\:"Like safe, and keep licence and copyright comments as well"
all\:"Remove every comment that no keep override protects"))' \
'--layout=[How the bytes left behind by a removed comment are laid out]:LAYOUT:((lines\:"Keep the line structure and separate tokens that would otherwise join"
columns\:"Pad each removed comment so the following columns do not shift"
compact\:"Drop lines that held only a removed comment, and the whitespace it left behind"))' \
'--language=[Force this language instead of detecting it from path and contents]:LANGUAGE:((rust\:"Rust source files"
ocaml\:"OCaml implementation and interface files"
c\:"C source and header files"
cpp\:"C++ source and header files"
go\:"Go source files"
java\:"Java source files, including Unicode escape translation"
javascript\:"JavaScript modules and scripts, including JSX"
typescript\:"TypeScript modules and scripts, including TSX"
python\:"Python source and stub files"
shell\:"POSIX sh, Bash, and zsh scripts"
html\:"HTML documents, including nested script and style elements"
css\:"CSS stylesheets"
jsonc\:"JSON with comments, including JSON5"
sql\:"SQL for every supported database dialect"
kotlin\:"Kotlin source and script files"
toml\:"TOML documents, including the lock files written in it"
lua\:"Lua chunks and LuaRocks rockspecs"
yaml\:"YAML documents, including the tool configurations written in it"
php\:"PHP scripts and templates; the inline HTML around the tags is content"
ruby\:"Ruby scripts, gem manifests, and the project files named after their tool"))' \
'--dialect=[Force this dialect of the selected language]:DIALECT:((standard\:"The default lexical rules of the language"
jsx\:"JavaScript with JSX elements"
tsx\:"TypeScript with JSX elements"
objective-c\:"Objective-C extensions to C"
objective-cpp\:"Objective-C++ extensions to C++"
gnu-c\:"GNU extensions to C"
gnu-cpp\:"GNU extensions to C++"
cuda\:"CUDA extensions to C++"
posix-sh\:"The POSIX shell command language"
bash53\:"Bash 5.3"
zsh\:"The Z shell"
postgresql\:"PostgreSQL, with dollar-quoted bodies"
mysql\:"MySQL, including its executable versioned comments"
sqlite\:"SQLite"
t-sql\:"Microsoft Transact-SQL"
oracle\:"Oracle SQL and PL/SQL"))' \
'*--keep-kind=[Comma-separated comment kinds to protect on top of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'*--remove-kind=[Comma-separated comment kinds to remove regardless of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'--format=[Output encoding]:FORMAT:(human json jsonl sarif github)' \
'--color=[When to colour terminal output]:WHEN:(auto always never)' \
'--hyperlinks=[When to emit terminal hyperlinks for reported paths]:WHEN:(auto always never)' \
'--progress=[When to draw the live scanning counter on standard error]:WHEN:(auto always never)' \
'--staged[Read and update Git index blobs rather than treating the working tree as the source]' \
'--index-only[With \`--staged\`, do not attempt a uniquely mappable working-tree update]' \
'--force-invalid[Apply the edits that are still provably safe when the source fails to scan]' \
'--force-protected[Remove protected comments such as shebang and encoding preambles]' \
'--no-preview[Omit the one-line comment text from human \`check\` and \`scan\` lines]' \
'--explain[List every comment human \`check\` and \`scan\` met and name the rule and setting behind each one]' \
'(-v --verbose)-q[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'(-v --verbose)--quiet[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'-v[Trace what is scanned and summarize every comment kind and skipped file]' \
'--verbose[Trace what is scanned and summarize every comment kind and skipped file]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'*::paths -- Files or directories to process; `-` reads standard input (default\: current directory):_files' \
&& ret=0
;;
(strip)
_arguments "${_arguments_options[@]}" : \
'--config=[Read this configuration file instead of discovering \`.ocomment.toml\`]:FILE:_files' \
'--policy=[Which classes of comment the run is allowed to remove]:POLICY:((safe\:"Remove ordinary and doc comments; keep preambles and directives"
legal\:"Like safe, and keep licence and copyright comments as well"
all\:"Remove every comment that no keep override protects"))' \
'--layout=[How the bytes left behind by a removed comment are laid out]:LAYOUT:((lines\:"Keep the line structure and separate tokens that would otherwise join"
columns\:"Pad each removed comment so the following columns do not shift"
compact\:"Drop lines that held only a removed comment, and the whitespace it left behind"))' \
'--language=[Force this language instead of detecting it from path and contents]:LANGUAGE:((rust\:"Rust source files"
ocaml\:"OCaml implementation and interface files"
c\:"C source and header files"
cpp\:"C++ source and header files"
go\:"Go source files"
java\:"Java source files, including Unicode escape translation"
javascript\:"JavaScript modules and scripts, including JSX"
typescript\:"TypeScript modules and scripts, including TSX"
python\:"Python source and stub files"
shell\:"POSIX sh, Bash, and zsh scripts"
html\:"HTML documents, including nested script and style elements"
css\:"CSS stylesheets"
jsonc\:"JSON with comments, including JSON5"
sql\:"SQL for every supported database dialect"
kotlin\:"Kotlin source and script files"
toml\:"TOML documents, including the lock files written in it"
lua\:"Lua chunks and LuaRocks rockspecs"
yaml\:"YAML documents, including the tool configurations written in it"
php\:"PHP scripts and templates; the inline HTML around the tags is content"
ruby\:"Ruby scripts, gem manifests, and the project files named after their tool"))' \
'--dialect=[Force this dialect of the selected language]:DIALECT:((standard\:"The default lexical rules of the language"
jsx\:"JavaScript with JSX elements"
tsx\:"TypeScript with JSX elements"
objective-c\:"Objective-C extensions to C"
objective-cpp\:"Objective-C++ extensions to C++"
gnu-c\:"GNU extensions to C"
gnu-cpp\:"GNU extensions to C++"
cuda\:"CUDA extensions to C++"
posix-sh\:"The POSIX shell command language"
bash53\:"Bash 5.3"
zsh\:"The Z shell"
postgresql\:"PostgreSQL, with dollar-quoted bodies"
mysql\:"MySQL, including its executable versioned comments"
sqlite\:"SQLite"
t-sql\:"Microsoft Transact-SQL"
oracle\:"Oracle SQL and PL/SQL"))' \
'*--keep-kind=[Comma-separated comment kinds to protect on top of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'*--remove-kind=[Comma-separated comment kinds to remove regardless of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'--format=[Output encoding]:FORMAT:(human json jsonl sarif github)' \
'--color=[When to colour terminal output]:WHEN:(auto always never)' \
'--hyperlinks=[When to emit terminal hyperlinks for reported paths]:WHEN:(auto always never)' \
'--progress=[When to draw the live scanning counter on standard error]:WHEN:(auto always never)' \
'--force-invalid[Apply the edits that are still provably safe when the source fails to scan]' \
'--force-protected[Remove protected comments such as shebang and encoding preambles]' \
'--no-preview[Omit the one-line comment text from human \`check\` and \`scan\` lines]' \
'--explain[List every comment human \`check\` and \`scan\` met and name the rule and setting behind each one]' \
'(-v --verbose)-q[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'(-v --verbose)--quiet[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'-v[Trace what is scanned and summarize every comment kind and skipped file]' \
'--verbose[Trace what is scanned and summarize every comment kind and skipped file]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(lsp)
_arguments "${_arguments_options[@]}" : \
'--config=[Read this configuration file instead of discovering \`.ocomment.toml\`]:FILE:_files' \
'--policy=[Which classes of comment the run is allowed to remove]:POLICY:((safe\:"Remove ordinary and doc comments; keep preambles and directives"
legal\:"Like safe, and keep licence and copyright comments as well"
all\:"Remove every comment that no keep override protects"))' \
'--layout=[How the bytes left behind by a removed comment are laid out]:LAYOUT:((lines\:"Keep the line structure and separate tokens that would otherwise join"
columns\:"Pad each removed comment so the following columns do not shift"
compact\:"Drop lines that held only a removed comment, and the whitespace it left behind"))' \
'--language=[Force this language instead of detecting it from path and contents]:LANGUAGE:((rust\:"Rust source files"
ocaml\:"OCaml implementation and interface files"
c\:"C source and header files"
cpp\:"C++ source and header files"
go\:"Go source files"
java\:"Java source files, including Unicode escape translation"
javascript\:"JavaScript modules and scripts, including JSX"
typescript\:"TypeScript modules and scripts, including TSX"
python\:"Python source and stub files"
shell\:"POSIX sh, Bash, and zsh scripts"
html\:"HTML documents, including nested script and style elements"
css\:"CSS stylesheets"
jsonc\:"JSON with comments, including JSON5"
sql\:"SQL for every supported database dialect"
kotlin\:"Kotlin source and script files"
toml\:"TOML documents, including the lock files written in it"
lua\:"Lua chunks and LuaRocks rockspecs"
yaml\:"YAML documents, including the tool configurations written in it"
php\:"PHP scripts and templates; the inline HTML around the tags is content"
ruby\:"Ruby scripts, gem manifests, and the project files named after their tool"))' \
'--dialect=[Force this dialect of the selected language]:DIALECT:((standard\:"The default lexical rules of the language"
jsx\:"JavaScript with JSX elements"
tsx\:"TypeScript with JSX elements"
objective-c\:"Objective-C extensions to C"
objective-cpp\:"Objective-C++ extensions to C++"
gnu-c\:"GNU extensions to C"
gnu-cpp\:"GNU extensions to C++"
cuda\:"CUDA extensions to C++"
posix-sh\:"The POSIX shell command language"
bash53\:"Bash 5.3"
zsh\:"The Z shell"
postgresql\:"PostgreSQL, with dollar-quoted bodies"
mysql\:"MySQL, including its executable versioned comments"
sqlite\:"SQLite"
t-sql\:"Microsoft Transact-SQL"
oracle\:"Oracle SQL and PL/SQL"))' \
'*--keep-kind=[Comma-separated comment kinds to protect on top of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'*--remove-kind=[Comma-separated comment kinds to remove regardless of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'--format=[Output encoding]:FORMAT:(human json jsonl sarif github)' \
'--color=[When to colour terminal output]:WHEN:(auto always never)' \
'--hyperlinks=[When to emit terminal hyperlinks for reported paths]:WHEN:(auto always never)' \
'--progress=[When to draw the live scanning counter on standard error]:WHEN:(auto always never)' \
'--force-invalid[Apply the edits that are still provably safe when the source fails to scan]' \
'--force-protected[Remove protected comments such as shebang and encoding preambles]' \
'--no-preview[Omit the one-line comment text from human \`check\` and \`scan\` lines]' \
'--explain[List every comment human \`check\` and \`scan\` met and name the rule and setting behind each one]' \
'(-v --verbose)-q[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'(-v --verbose)--quiet[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'-v[Trace what is scanned and summarize every comment kind and skipped file]' \
'--verbose[Trace what is scanned and summarize every comment kind and skipped file]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(init)
_arguments "${_arguments_options[@]}" : \
'--config=[Read this configuration file instead of discovering \`.ocomment.toml\`]:FILE:_files' \
'--policy=[Which classes of comment the run is allowed to remove]:POLICY:((safe\:"Remove ordinary and doc comments; keep preambles and directives"
legal\:"Like safe, and keep licence and copyright comments as well"
all\:"Remove every comment that no keep override protects"))' \
'--layout=[How the bytes left behind by a removed comment are laid out]:LAYOUT:((lines\:"Keep the line structure and separate tokens that would otherwise join"
columns\:"Pad each removed comment so the following columns do not shift"
compact\:"Drop lines that held only a removed comment, and the whitespace it left behind"))' \
'--language=[Force this language instead of detecting it from path and contents]:LANGUAGE:((rust\:"Rust source files"
ocaml\:"OCaml implementation and interface files"
c\:"C source and header files"
cpp\:"C++ source and header files"
go\:"Go source files"
java\:"Java source files, including Unicode escape translation"
javascript\:"JavaScript modules and scripts, including JSX"
typescript\:"TypeScript modules and scripts, including TSX"
python\:"Python source and stub files"
shell\:"POSIX sh, Bash, and zsh scripts"
html\:"HTML documents, including nested script and style elements"
css\:"CSS stylesheets"
jsonc\:"JSON with comments, including JSON5"
sql\:"SQL for every supported database dialect"
kotlin\:"Kotlin source and script files"
toml\:"TOML documents, including the lock files written in it"
lua\:"Lua chunks and LuaRocks rockspecs"
yaml\:"YAML documents, including the tool configurations written in it"
php\:"PHP scripts and templates; the inline HTML around the tags is content"
ruby\:"Ruby scripts, gem manifests, and the project files named after their tool"))' \
'--dialect=[Force this dialect of the selected language]:DIALECT:((standard\:"The default lexical rules of the language"
jsx\:"JavaScript with JSX elements"
tsx\:"TypeScript with JSX elements"
objective-c\:"Objective-C extensions to C"
objective-cpp\:"Objective-C++ extensions to C++"
gnu-c\:"GNU extensions to C"
gnu-cpp\:"GNU extensions to C++"
cuda\:"CUDA extensions to C++"
posix-sh\:"The POSIX shell command language"
bash53\:"Bash 5.3"
zsh\:"The Z shell"
postgresql\:"PostgreSQL, with dollar-quoted bodies"
mysql\:"MySQL, including its executable versioned comments"
sqlite\:"SQLite"
t-sql\:"Microsoft Transact-SQL"
oracle\:"Oracle SQL and PL/SQL"))' \
'*--keep-kind=[Comma-separated comment kinds to protect on top of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'*--remove-kind=[Comma-separated comment kinds to remove regardless of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'--format=[Output encoding]:FORMAT:(human json jsonl sarif github)' \
'--color=[When to colour terminal output]:WHEN:(auto always never)' \
'--hyperlinks=[When to emit terminal hyperlinks for reported paths]:WHEN:(auto always never)' \
'--progress=[When to draw the live scanning counter on standard error]:WHEN:(auto always never)' \
'--fix[For the Lefthook hook, run \`fix\` instead of \`check\`]' \
'(--stdout)--force[Replace the file if it already exists]' \
'--stdout[Print the template to standard output and write no file]' \
'--force-invalid[Apply the edits that are still provably safe when the source fails to scan]' \
'--force-protected[Remove protected comments such as shebang and encoding preambles]' \
'--no-preview[Omit the one-line comment text from human \`check\` and \`scan\` lines]' \
'--explain[List every comment human \`check\` and \`scan\` met and name the rule and setting behind each one]' \
'(-v --verbose)-q[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'(-v --verbose)--quiet[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'-v[Trace what is scanned and summarize every comment kind and skipped file]' \
'--verbose[Trace what is scanned and summarize every comment kind and skipped file]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::kind -- Which starter file to write:(config lefthook)' \
&& ret=0
;;
(config)
_arguments "${_arguments_options[@]}" : \
'--config=[Read this configuration file instead of discovering \`.ocomment.toml\`]:FILE:_files' \
'--policy=[Which classes of comment the run is allowed to remove]:POLICY:((safe\:"Remove ordinary and doc comments; keep preambles and directives"
legal\:"Like safe, and keep licence and copyright comments as well"
all\:"Remove every comment that no keep override protects"))' \
'--layout=[How the bytes left behind by a removed comment are laid out]:LAYOUT:((lines\:"Keep the line structure and separate tokens that would otherwise join"
columns\:"Pad each removed comment so the following columns do not shift"
compact\:"Drop lines that held only a removed comment, and the whitespace it left behind"))' \
'--language=[Force this language instead of detecting it from path and contents]:LANGUAGE:((rust\:"Rust source files"
ocaml\:"OCaml implementation and interface files"
c\:"C source and header files"
cpp\:"C++ source and header files"
go\:"Go source files"
java\:"Java source files, including Unicode escape translation"
javascript\:"JavaScript modules and scripts, including JSX"
typescript\:"TypeScript modules and scripts, including TSX"
python\:"Python source and stub files"
shell\:"POSIX sh, Bash, and zsh scripts"
html\:"HTML documents, including nested script and style elements"
css\:"CSS stylesheets"
jsonc\:"JSON with comments, including JSON5"
sql\:"SQL for every supported database dialect"
kotlin\:"Kotlin source and script files"
toml\:"TOML documents, including the lock files written in it"
lua\:"Lua chunks and LuaRocks rockspecs"
yaml\:"YAML documents, including the tool configurations written in it"
php\:"PHP scripts and templates; the inline HTML around the tags is content"
ruby\:"Ruby scripts, gem manifests, and the project files named after their tool"))' \
'--dialect=[Force this dialect of the selected language]:DIALECT:((standard\:"The default lexical rules of the language"
jsx\:"JavaScript with JSX elements"
tsx\:"TypeScript with JSX elements"
objective-c\:"Objective-C extensions to C"
objective-cpp\:"Objective-C++ extensions to C++"
gnu-c\:"GNU extensions to C"
gnu-cpp\:"GNU extensions to C++"
cuda\:"CUDA extensions to C++"
posix-sh\:"The POSIX shell command language"
bash53\:"Bash 5.3"
zsh\:"The Z shell"
postgresql\:"PostgreSQL, with dollar-quoted bodies"
mysql\:"MySQL, including its executable versioned comments"
sqlite\:"SQLite"
t-sql\:"Microsoft Transact-SQL"
oracle\:"Oracle SQL and PL/SQL"))' \
'*--keep-kind=[Comma-separated comment kinds to protect on top of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'*--remove-kind=[Comma-separated comment kinds to remove regardless of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'--format=[Output encoding]:FORMAT:(human json jsonl sarif github)' \
'--color=[When to colour terminal output]:WHEN:(auto always never)' \
'--hyperlinks=[When to emit terminal hyperlinks for reported paths]:WHEN:(auto always never)' \
'--progress=[When to draw the live scanning counter on standard error]:WHEN:(auto always never)' \
'--force-invalid[Apply the edits that are still provably safe when the source fails to scan]' \
'--force-protected[Remove protected comments such as shebang and encoding preambles]' \
'--no-preview[Omit the one-line comment text from human \`check\` and \`scan\` lines]' \
'--explain[List every comment human \`check\` and \`scan\` met and name the rule and setting behind each one]' \
'(-v --verbose)-q[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'(-v --verbose)--quiet[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'-v[Trace what is scanned and summarize every comment kind and skipped file]' \
'--verbose[Trace what is scanned and summarize every comment kind and skipped file]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::action -- Which view of the resolved configuration to print:(show locate explain schema)' \
&& ret=0
;;
(languages)
_arguments "${_arguments_options[@]}" : \
'--config=[Read this configuration file instead of discovering \`.ocomment.toml\`]:FILE:_files' \
'--policy=[Which classes of comment the run is allowed to remove]:POLICY:((safe\:"Remove ordinary and doc comments; keep preambles and directives"
legal\:"Like safe, and keep licence and copyright comments as well"
all\:"Remove every comment that no keep override protects"))' \
'--layout=[How the bytes left behind by a removed comment are laid out]:LAYOUT:((lines\:"Keep the line structure and separate tokens that would otherwise join"
columns\:"Pad each removed comment so the following columns do not shift"
compact\:"Drop lines that held only a removed comment, and the whitespace it left behind"))' \
'--language=[Force this language instead of detecting it from path and contents]:LANGUAGE:((rust\:"Rust source files"
ocaml\:"OCaml implementation and interface files"
c\:"C source and header files"
cpp\:"C++ source and header files"
go\:"Go source files"
java\:"Java source files, including Unicode escape translation"
javascript\:"JavaScript modules and scripts, including JSX"
typescript\:"TypeScript modules and scripts, including TSX"
python\:"Python source and stub files"
shell\:"POSIX sh, Bash, and zsh scripts"
html\:"HTML documents, including nested script and style elements"
css\:"CSS stylesheets"
jsonc\:"JSON with comments, including JSON5"
sql\:"SQL for every supported database dialect"
kotlin\:"Kotlin source and script files"
toml\:"TOML documents, including the lock files written in it"
lua\:"Lua chunks and LuaRocks rockspecs"
yaml\:"YAML documents, including the tool configurations written in it"
php\:"PHP scripts and templates; the inline HTML around the tags is content"
ruby\:"Ruby scripts, gem manifests, and the project files named after their tool"))' \
'--dialect=[Force this dialect of the selected language]:DIALECT:((standard\:"The default lexical rules of the language"
jsx\:"JavaScript with JSX elements"
tsx\:"TypeScript with JSX elements"
objective-c\:"Objective-C extensions to C"
objective-cpp\:"Objective-C++ extensions to C++"
gnu-c\:"GNU extensions to C"
gnu-cpp\:"GNU extensions to C++"
cuda\:"CUDA extensions to C++"
posix-sh\:"The POSIX shell command language"
bash53\:"Bash 5.3"
zsh\:"The Z shell"
postgresql\:"PostgreSQL, with dollar-quoted bodies"
mysql\:"MySQL, including its executable versioned comments"
sqlite\:"SQLite"
t-sql\:"Microsoft Transact-SQL"
oracle\:"Oracle SQL and PL/SQL"))' \
'*--keep-kind=[Comma-separated comment kinds to protect on top of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'*--remove-kind=[Comma-separated comment kinds to remove regardless of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'--format=[Output encoding]:FORMAT:(human json jsonl sarif github)' \
'--color=[When to colour terminal output]:WHEN:(auto always never)' \
'--hyperlinks=[When to emit terminal hyperlinks for reported paths]:WHEN:(auto always never)' \
'--progress=[When to draw the live scanning counter on standard error]:WHEN:(auto always never)' \
'--force-invalid[Apply the edits that are still provably safe when the source fails to scan]' \
'--force-protected[Remove protected comments such as shebang and encoding preambles]' \
'--no-preview[Omit the one-line comment text from human \`check\` and \`scan\` lines]' \
'--explain[List every comment human \`check\` and \`scan\` met and name the rule and setting behind each one]' \
'(-v --verbose)-q[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'(-v --verbose)--quiet[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'-v[Trace what is scanned and summarize every comment kind and skipped file]' \
'--verbose[Trace what is scanned and summarize every comment kind and skipped file]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(plugin)
_arguments "${_arguments_options[@]}" : \
'--config=[Read this configuration file instead of discovering \`.ocomment.toml\`]:FILE:_files' \
'--policy=[Which classes of comment the run is allowed to remove]:POLICY:((safe\:"Remove ordinary and doc comments; keep preambles and directives"
legal\:"Like safe, and keep licence and copyright comments as well"
all\:"Remove every comment that no keep override protects"))' \
'--layout=[How the bytes left behind by a removed comment are laid out]:LAYOUT:((lines\:"Keep the line structure and separate tokens that would otherwise join"
columns\:"Pad each removed comment so the following columns do not shift"
compact\:"Drop lines that held only a removed comment, and the whitespace it left behind"))' \
'--language=[Force this language instead of detecting it from path and contents]:LANGUAGE:((rust\:"Rust source files"
ocaml\:"OCaml implementation and interface files"
c\:"C source and header files"
cpp\:"C++ source and header files"
go\:"Go source files"
java\:"Java source files, including Unicode escape translation"
javascript\:"JavaScript modules and scripts, including JSX"
typescript\:"TypeScript modules and scripts, including TSX"
python\:"Python source and stub files"
shell\:"POSIX sh, Bash, and zsh scripts"
html\:"HTML documents, including nested script and style elements"
css\:"CSS stylesheets"
jsonc\:"JSON with comments, including JSON5"
sql\:"SQL for every supported database dialect"
kotlin\:"Kotlin source and script files"
toml\:"TOML documents, including the lock files written in it"
lua\:"Lua chunks and LuaRocks rockspecs"
yaml\:"YAML documents, including the tool configurations written in it"
php\:"PHP scripts and templates; the inline HTML around the tags is content"
ruby\:"Ruby scripts, gem manifests, and the project files named after their tool"))' \
'--dialect=[Force this dialect of the selected language]:DIALECT:((standard\:"The default lexical rules of the language"
jsx\:"JavaScript with JSX elements"
tsx\:"TypeScript with JSX elements"
objective-c\:"Objective-C extensions to C"
objective-cpp\:"Objective-C++ extensions to C++"
gnu-c\:"GNU extensions to C"
gnu-cpp\:"GNU extensions to C++"
cuda\:"CUDA extensions to C++"
posix-sh\:"The POSIX shell command language"
bash53\:"Bash 5.3"
zsh\:"The Z shell"
postgresql\:"PostgreSQL, with dollar-quoted bodies"
mysql\:"MySQL, including its executable versioned comments"
sqlite\:"SQLite"
t-sql\:"Microsoft Transact-SQL"
oracle\:"Oracle SQL and PL/SQL"))' \
'*--keep-kind=[Comma-separated comment kinds to protect on top of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'*--remove-kind=[Comma-separated comment kinds to remove regardless of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'--format=[Output encoding]:FORMAT:(human json jsonl sarif github)' \
'--color=[When to colour terminal output]:WHEN:(auto always never)' \
'--hyperlinks=[When to emit terminal hyperlinks for reported paths]:WHEN:(auto always never)' \
'--progress=[When to draw the live scanning counter on standard error]:WHEN:(auto always never)' \
'--force-invalid[Apply the edits that are still provably safe when the source fails to scan]' \
'--force-protected[Remove protected comments such as shebang and encoding preambles]' \
'--no-preview[Omit the one-line comment text from human \`check\` and \`scan\` lines]' \
'--explain[List every comment human \`check\` and \`scan\` met and name the rule and setting behind each one]' \
'(-v --verbose)-q[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'(-v --verbose)--quiet[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'-v[Trace what is scanned and summarize every comment kind and skipped file]' \
'--verbose[Trace what is scanned and summarize every comment kind and skipped file]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
":: :_ocomment__subcmd__plugin_commands" \
"*::: :->plugin" \
&& ret=0

    case $state in
    (plugin)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:ocomment-plugin-command-$line[1]:"
        case $line[1] in
            (add)
_arguments "${_arguments_options[@]}" : \
'--name=[Name to register the plugin under (default\: the file stem)]:NAME:_default' \
'--sha256=[Expected SHA-256 digest of the component, verified before install]:HEX:_default' \
'--identity=[Publisher identity recorded alongside the pinned digest]:IDENTITY:_default' \
'--config=[Read this configuration file instead of discovering \`.ocomment.toml\`]:FILE:_files' \
'--policy=[Which classes of comment the run is allowed to remove]:POLICY:((safe\:"Remove ordinary and doc comments; keep preambles and directives"
legal\:"Like safe, and keep licence and copyright comments as well"
all\:"Remove every comment that no keep override protects"))' \
'--layout=[How the bytes left behind by a removed comment are laid out]:LAYOUT:((lines\:"Keep the line structure and separate tokens that would otherwise join"
columns\:"Pad each removed comment so the following columns do not shift"
compact\:"Drop lines that held only a removed comment, and the whitespace it left behind"))' \
'--language=[Force this language instead of detecting it from path and contents]:LANGUAGE:((rust\:"Rust source files"
ocaml\:"OCaml implementation and interface files"
c\:"C source and header files"
cpp\:"C++ source and header files"
go\:"Go source files"
java\:"Java source files, including Unicode escape translation"
javascript\:"JavaScript modules and scripts, including JSX"
typescript\:"TypeScript modules and scripts, including TSX"
python\:"Python source and stub files"
shell\:"POSIX sh, Bash, and zsh scripts"
html\:"HTML documents, including nested script and style elements"
css\:"CSS stylesheets"
jsonc\:"JSON with comments, including JSON5"
sql\:"SQL for every supported database dialect"
kotlin\:"Kotlin source and script files"
toml\:"TOML documents, including the lock files written in it"
lua\:"Lua chunks and LuaRocks rockspecs"
yaml\:"YAML documents, including the tool configurations written in it"
php\:"PHP scripts and templates; the inline HTML around the tags is content"
ruby\:"Ruby scripts, gem manifests, and the project files named after their tool"))' \
'--dialect=[Force this dialect of the selected language]:DIALECT:((standard\:"The default lexical rules of the language"
jsx\:"JavaScript with JSX elements"
tsx\:"TypeScript with JSX elements"
objective-c\:"Objective-C extensions to C"
objective-cpp\:"Objective-C++ extensions to C++"
gnu-c\:"GNU extensions to C"
gnu-cpp\:"GNU extensions to C++"
cuda\:"CUDA extensions to C++"
posix-sh\:"The POSIX shell command language"
bash53\:"Bash 5.3"
zsh\:"The Z shell"
postgresql\:"PostgreSQL, with dollar-quoted bodies"
mysql\:"MySQL, including its executable versioned comments"
sqlite\:"SQLite"
t-sql\:"Microsoft Transact-SQL"
oracle\:"Oracle SQL and PL/SQL"))' \
'*--keep-kind=[Comma-separated comment kinds to protect on top of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'*--remove-kind=[Comma-separated comment kinds to remove regardless of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'--format=[Output encoding]:FORMAT:(human json jsonl sarif github)' \
'--color=[When to colour terminal output]:WHEN:(auto always never)' \
'--hyperlinks=[When to emit terminal hyperlinks for reported paths]:WHEN:(auto always never)' \
'--progress=[When to draw the live scanning counter on standard error]:WHEN:(auto always never)' \
'--force-invalid[Apply the edits that are still provably safe when the source fails to scan]' \
'--force-protected[Remove protected comments such as shebang and encoding preambles]' \
'--no-preview[Omit the one-line comment text from human \`check\` and \`scan\` lines]' \
'--explain[List every comment human \`check\` and \`scan\` met and name the rule and setting behind each one]' \
'(-v --verbose)-q[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'(-v --verbose)--quiet[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'-v[Trace what is scanned and summarize every comment kind and skipped file]' \
'--verbose[Trace what is scanned and summarize every comment kind and skipped file]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':source -- Path or URL of the WASM component to install:_default' \
&& ret=0
;;
(remove)
_arguments "${_arguments_options[@]}" : \
'--config=[Read this configuration file instead of discovering \`.ocomment.toml\`]:FILE:_files' \
'--policy=[Which classes of comment the run is allowed to remove]:POLICY:((safe\:"Remove ordinary and doc comments; keep preambles and directives"
legal\:"Like safe, and keep licence and copyright comments as well"
all\:"Remove every comment that no keep override protects"))' \
'--layout=[How the bytes left behind by a removed comment are laid out]:LAYOUT:((lines\:"Keep the line structure and separate tokens that would otherwise join"
columns\:"Pad each removed comment so the following columns do not shift"
compact\:"Drop lines that held only a removed comment, and the whitespace it left behind"))' \
'--language=[Force this language instead of detecting it from path and contents]:LANGUAGE:((rust\:"Rust source files"
ocaml\:"OCaml implementation and interface files"
c\:"C source and header files"
cpp\:"C++ source and header files"
go\:"Go source files"
java\:"Java source files, including Unicode escape translation"
javascript\:"JavaScript modules and scripts, including JSX"
typescript\:"TypeScript modules and scripts, including TSX"
python\:"Python source and stub files"
shell\:"POSIX sh, Bash, and zsh scripts"
html\:"HTML documents, including nested script and style elements"
css\:"CSS stylesheets"
jsonc\:"JSON with comments, including JSON5"
sql\:"SQL for every supported database dialect"
kotlin\:"Kotlin source and script files"
toml\:"TOML documents, including the lock files written in it"
lua\:"Lua chunks and LuaRocks rockspecs"
yaml\:"YAML documents, including the tool configurations written in it"
php\:"PHP scripts and templates; the inline HTML around the tags is content"
ruby\:"Ruby scripts, gem manifests, and the project files named after their tool"))' \
'--dialect=[Force this dialect of the selected language]:DIALECT:((standard\:"The default lexical rules of the language"
jsx\:"JavaScript with JSX elements"
tsx\:"TypeScript with JSX elements"
objective-c\:"Objective-C extensions to C"
objective-cpp\:"Objective-C++ extensions to C++"
gnu-c\:"GNU extensions to C"
gnu-cpp\:"GNU extensions to C++"
cuda\:"CUDA extensions to C++"
posix-sh\:"The POSIX shell command language"
bash53\:"Bash 5.3"
zsh\:"The Z shell"
postgresql\:"PostgreSQL, with dollar-quoted bodies"
mysql\:"MySQL, including its executable versioned comments"
sqlite\:"SQLite"
t-sql\:"Microsoft Transact-SQL"
oracle\:"Oracle SQL and PL/SQL"))' \
'*--keep-kind=[Comma-separated comment kinds to protect on top of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'*--remove-kind=[Comma-separated comment kinds to remove regardless of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'--format=[Output encoding]:FORMAT:(human json jsonl sarif github)' \
'--color=[When to colour terminal output]:WHEN:(auto always never)' \
'--hyperlinks=[When to emit terminal hyperlinks for reported paths]:WHEN:(auto always never)' \
'--progress=[When to draw the live scanning counter on standard error]:WHEN:(auto always never)' \
'--force-invalid[Apply the edits that are still provably safe when the source fails to scan]' \
'--force-protected[Remove protected comments such as shebang and encoding preambles]' \
'--no-preview[Omit the one-line comment text from human \`check\` and \`scan\` lines]' \
'--explain[List every comment human \`check\` and \`scan\` met and name the rule and setting behind each one]' \
'(-v --verbose)-q[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'(-v --verbose)--quiet[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'-v[Trace what is scanned and summarize every comment kind and skipped file]' \
'--verbose[Trace what is scanned and summarize every comment kind and skipped file]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':name -- Name of the plugin to remove:_default' \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" : \
'--config=[Read this configuration file instead of discovering \`.ocomment.toml\`]:FILE:_files' \
'--policy=[Which classes of comment the run is allowed to remove]:POLICY:((safe\:"Remove ordinary and doc comments; keep preambles and directives"
legal\:"Like safe, and keep licence and copyright comments as well"
all\:"Remove every comment that no keep override protects"))' \
'--layout=[How the bytes left behind by a removed comment are laid out]:LAYOUT:((lines\:"Keep the line structure and separate tokens that would otherwise join"
columns\:"Pad each removed comment so the following columns do not shift"
compact\:"Drop lines that held only a removed comment, and the whitespace it left behind"))' \
'--language=[Force this language instead of detecting it from path and contents]:LANGUAGE:((rust\:"Rust source files"
ocaml\:"OCaml implementation and interface files"
c\:"C source and header files"
cpp\:"C++ source and header files"
go\:"Go source files"
java\:"Java source files, including Unicode escape translation"
javascript\:"JavaScript modules and scripts, including JSX"
typescript\:"TypeScript modules and scripts, including TSX"
python\:"Python source and stub files"
shell\:"POSIX sh, Bash, and zsh scripts"
html\:"HTML documents, including nested script and style elements"
css\:"CSS stylesheets"
jsonc\:"JSON with comments, including JSON5"
sql\:"SQL for every supported database dialect"
kotlin\:"Kotlin source and script files"
toml\:"TOML documents, including the lock files written in it"
lua\:"Lua chunks and LuaRocks rockspecs"
yaml\:"YAML documents, including the tool configurations written in it"
php\:"PHP scripts and templates; the inline HTML around the tags is content"
ruby\:"Ruby scripts, gem manifests, and the project files named after their tool"))' \
'--dialect=[Force this dialect of the selected language]:DIALECT:((standard\:"The default lexical rules of the language"
jsx\:"JavaScript with JSX elements"
tsx\:"TypeScript with JSX elements"
objective-c\:"Objective-C extensions to C"
objective-cpp\:"Objective-C++ extensions to C++"
gnu-c\:"GNU extensions to C"
gnu-cpp\:"GNU extensions to C++"
cuda\:"CUDA extensions to C++"
posix-sh\:"The POSIX shell command language"
bash53\:"Bash 5.3"
zsh\:"The Z shell"
postgresql\:"PostgreSQL, with dollar-quoted bodies"
mysql\:"MySQL, including its executable versioned comments"
sqlite\:"SQLite"
t-sql\:"Microsoft Transact-SQL"
oracle\:"Oracle SQL and PL/SQL"))' \
'*--keep-kind=[Comma-separated comment kinds to protect on top of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'*--remove-kind=[Comma-separated comment kinds to remove regardless of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'--format=[Output encoding]:FORMAT:(human json jsonl sarif github)' \
'--color=[When to colour terminal output]:WHEN:(auto always never)' \
'--hyperlinks=[When to emit terminal hyperlinks for reported paths]:WHEN:(auto always never)' \
'--progress=[When to draw the live scanning counter on standard error]:WHEN:(auto always never)' \
'--force-invalid[Apply the edits that are still provably safe when the source fails to scan]' \
'--force-protected[Remove protected comments such as shebang and encoding preambles]' \
'--no-preview[Omit the one-line comment text from human \`check\` and \`scan\` lines]' \
'--explain[List every comment human \`check\` and \`scan\` met and name the rule and setting behind each one]' \
'(-v --verbose)-q[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'(-v --verbose)--quiet[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'-v[Trace what is scanned and summarize every comment kind and skipped file]' \
'--verbose[Trace what is scanned and summarize every comment kind and skipped file]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(update)
_arguments "${_arguments_options[@]}" : \
'--config=[Read this configuration file instead of discovering \`.ocomment.toml\`]:FILE:_files' \
'--policy=[Which classes of comment the run is allowed to remove]:POLICY:((safe\:"Remove ordinary and doc comments; keep preambles and directives"
legal\:"Like safe, and keep licence and copyright comments as well"
all\:"Remove every comment that no keep override protects"))' \
'--layout=[How the bytes left behind by a removed comment are laid out]:LAYOUT:((lines\:"Keep the line structure and separate tokens that would otherwise join"
columns\:"Pad each removed comment so the following columns do not shift"
compact\:"Drop lines that held only a removed comment, and the whitespace it left behind"))' \
'--language=[Force this language instead of detecting it from path and contents]:LANGUAGE:((rust\:"Rust source files"
ocaml\:"OCaml implementation and interface files"
c\:"C source and header files"
cpp\:"C++ source and header files"
go\:"Go source files"
java\:"Java source files, including Unicode escape translation"
javascript\:"JavaScript modules and scripts, including JSX"
typescript\:"TypeScript modules and scripts, including TSX"
python\:"Python source and stub files"
shell\:"POSIX sh, Bash, and zsh scripts"
html\:"HTML documents, including nested script and style elements"
css\:"CSS stylesheets"
jsonc\:"JSON with comments, including JSON5"
sql\:"SQL for every supported database dialect"
kotlin\:"Kotlin source and script files"
toml\:"TOML documents, including the lock files written in it"
lua\:"Lua chunks and LuaRocks rockspecs"
yaml\:"YAML documents, including the tool configurations written in it"
php\:"PHP scripts and templates; the inline HTML around the tags is content"
ruby\:"Ruby scripts, gem manifests, and the project files named after their tool"))' \
'--dialect=[Force this dialect of the selected language]:DIALECT:((standard\:"The default lexical rules of the language"
jsx\:"JavaScript with JSX elements"
tsx\:"TypeScript with JSX elements"
objective-c\:"Objective-C extensions to C"
objective-cpp\:"Objective-C++ extensions to C++"
gnu-c\:"GNU extensions to C"
gnu-cpp\:"GNU extensions to C++"
cuda\:"CUDA extensions to C++"
posix-sh\:"The POSIX shell command language"
bash53\:"Bash 5.3"
zsh\:"The Z shell"
postgresql\:"PostgreSQL, with dollar-quoted bodies"
mysql\:"MySQL, including its executable versioned comments"
sqlite\:"SQLite"
t-sql\:"Microsoft Transact-SQL"
oracle\:"Oracle SQL and PL/SQL"))' \
'*--keep-kind=[Comma-separated comment kinds to protect on top of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'*--remove-kind=[Comma-separated comment kinds to remove regardless of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'--format=[Output encoding]:FORMAT:(human json jsonl sarif github)' \
'--color=[When to colour terminal output]:WHEN:(auto always never)' \
'--hyperlinks=[When to emit terminal hyperlinks for reported paths]:WHEN:(auto always never)' \
'--progress=[When to draw the live scanning counter on standard error]:WHEN:(auto always never)' \
'--force-invalid[Apply the edits that are still provably safe when the source fails to scan]' \
'--force-protected[Remove protected comments such as shebang and encoding preambles]' \
'--no-preview[Omit the one-line comment text from human \`check\` and \`scan\` lines]' \
'--explain[List every comment human \`check\` and \`scan\` met and name the rule and setting behind each one]' \
'(-v --verbose)-q[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'(-v --verbose)--quiet[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'-v[Trace what is scanned and summarize every comment kind and skipped file]' \
'--verbose[Trace what is scanned and summarize every comment kind and skipped file]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::name -- Name of the plugin to update (default\: all of them):_default' \
&& ret=0
;;
(verify)
_arguments "${_arguments_options[@]}" : \
'--config=[Read this configuration file instead of discovering \`.ocomment.toml\`]:FILE:_files' \
'--policy=[Which classes of comment the run is allowed to remove]:POLICY:((safe\:"Remove ordinary and doc comments; keep preambles and directives"
legal\:"Like safe, and keep licence and copyright comments as well"
all\:"Remove every comment that no keep override protects"))' \
'--layout=[How the bytes left behind by a removed comment are laid out]:LAYOUT:((lines\:"Keep the line structure and separate tokens that would otherwise join"
columns\:"Pad each removed comment so the following columns do not shift"
compact\:"Drop lines that held only a removed comment, and the whitespace it left behind"))' \
'--language=[Force this language instead of detecting it from path and contents]:LANGUAGE:((rust\:"Rust source files"
ocaml\:"OCaml implementation and interface files"
c\:"C source and header files"
cpp\:"C++ source and header files"
go\:"Go source files"
java\:"Java source files, including Unicode escape translation"
javascript\:"JavaScript modules and scripts, including JSX"
typescript\:"TypeScript modules and scripts, including TSX"
python\:"Python source and stub files"
shell\:"POSIX sh, Bash, and zsh scripts"
html\:"HTML documents, including nested script and style elements"
css\:"CSS stylesheets"
jsonc\:"JSON with comments, including JSON5"
sql\:"SQL for every supported database dialect"
kotlin\:"Kotlin source and script files"
toml\:"TOML documents, including the lock files written in it"
lua\:"Lua chunks and LuaRocks rockspecs"
yaml\:"YAML documents, including the tool configurations written in it"
php\:"PHP scripts and templates; the inline HTML around the tags is content"
ruby\:"Ruby scripts, gem manifests, and the project files named after their tool"))' \
'--dialect=[Force this dialect of the selected language]:DIALECT:((standard\:"The default lexical rules of the language"
jsx\:"JavaScript with JSX elements"
tsx\:"TypeScript with JSX elements"
objective-c\:"Objective-C extensions to C"
objective-cpp\:"Objective-C++ extensions to C++"
gnu-c\:"GNU extensions to C"
gnu-cpp\:"GNU extensions to C++"
cuda\:"CUDA extensions to C++"
posix-sh\:"The POSIX shell command language"
bash53\:"Bash 5.3"
zsh\:"The Z shell"
postgresql\:"PostgreSQL, with dollar-quoted bodies"
mysql\:"MySQL, including its executable versioned comments"
sqlite\:"SQLite"
t-sql\:"Microsoft Transact-SQL"
oracle\:"Oracle SQL and PL/SQL"))' \
'*--keep-kind=[Comma-separated comment kinds to protect on top of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'*--remove-kind=[Comma-separated comment kinds to remove regardless of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'--format=[Output encoding]:FORMAT:(human json jsonl sarif github)' \
'--color=[When to colour terminal output]:WHEN:(auto always never)' \
'--hyperlinks=[When to emit terminal hyperlinks for reported paths]:WHEN:(auto always never)' \
'--progress=[When to draw the live scanning counter on standard error]:WHEN:(auto always never)' \
'--force-invalid[Apply the edits that are still provably safe when the source fails to scan]' \
'--force-protected[Remove protected comments such as shebang and encoding preambles]' \
'--no-preview[Omit the one-line comment text from human \`check\` and \`scan\` lines]' \
'--explain[List every comment human \`check\` and \`scan\` met and name the rule and setting behind each one]' \
'(-v --verbose)-q[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'(-v --verbose)--quiet[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'-v[Trace what is scanned and summarize every comment kind and skipped file]' \
'--verbose[Trace what is scanned and summarize every comment kind and skipped file]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::name -- Name of the plugin to verify (default\: all of them):_default' \
&& ret=0
;;
(new)
_arguments "${_arguments_options[@]}" : \
'--config=[Read this configuration file instead of discovering \`.ocomment.toml\`]:FILE:_files' \
'--policy=[Which classes of comment the run is allowed to remove]:POLICY:((safe\:"Remove ordinary and doc comments; keep preambles and directives"
legal\:"Like safe, and keep licence and copyright comments as well"
all\:"Remove every comment that no keep override protects"))' \
'--layout=[How the bytes left behind by a removed comment are laid out]:LAYOUT:((lines\:"Keep the line structure and separate tokens that would otherwise join"
columns\:"Pad each removed comment so the following columns do not shift"
compact\:"Drop lines that held only a removed comment, and the whitespace it left behind"))' \
'--language=[Force this language instead of detecting it from path and contents]:LANGUAGE:((rust\:"Rust source files"
ocaml\:"OCaml implementation and interface files"
c\:"C source and header files"
cpp\:"C++ source and header files"
go\:"Go source files"
java\:"Java source files, including Unicode escape translation"
javascript\:"JavaScript modules and scripts, including JSX"
typescript\:"TypeScript modules and scripts, including TSX"
python\:"Python source and stub files"
shell\:"POSIX sh, Bash, and zsh scripts"
html\:"HTML documents, including nested script and style elements"
css\:"CSS stylesheets"
jsonc\:"JSON with comments, including JSON5"
sql\:"SQL for every supported database dialect"
kotlin\:"Kotlin source and script files"
toml\:"TOML documents, including the lock files written in it"
lua\:"Lua chunks and LuaRocks rockspecs"
yaml\:"YAML documents, including the tool configurations written in it"
php\:"PHP scripts and templates; the inline HTML around the tags is content"
ruby\:"Ruby scripts, gem manifests, and the project files named after their tool"))' \
'--dialect=[Force this dialect of the selected language]:DIALECT:((standard\:"The default lexical rules of the language"
jsx\:"JavaScript with JSX elements"
tsx\:"TypeScript with JSX elements"
objective-c\:"Objective-C extensions to C"
objective-cpp\:"Objective-C++ extensions to C++"
gnu-c\:"GNU extensions to C"
gnu-cpp\:"GNU extensions to C++"
cuda\:"CUDA extensions to C++"
posix-sh\:"The POSIX shell command language"
bash53\:"Bash 5.3"
zsh\:"The Z shell"
postgresql\:"PostgreSQL, with dollar-quoted bodies"
mysql\:"MySQL, including its executable versioned comments"
sqlite\:"SQLite"
t-sql\:"Microsoft Transact-SQL"
oracle\:"Oracle SQL and PL/SQL"))' \
'*--keep-kind=[Comma-separated comment kinds to protect on top of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'*--remove-kind=[Comma-separated comment kinds to remove regardless of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'--format=[Output encoding]:FORMAT:(human json jsonl sarif github)' \
'--color=[When to colour terminal output]:WHEN:(auto always never)' \
'--hyperlinks=[When to emit terminal hyperlinks for reported paths]:WHEN:(auto always never)' \
'--progress=[When to draw the live scanning counter on standard error]:WHEN:(auto always never)' \
'--force-invalid[Apply the edits that are still provably safe when the source fails to scan]' \
'--force-protected[Remove protected comments such as shebang and encoding preambles]' \
'--no-preview[Omit the one-line comment text from human \`check\` and \`scan\` lines]' \
'--explain[List every comment human \`check\` and \`scan\` met and name the rule and setting behind each one]' \
'(-v --verbose)-q[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'(-v --verbose)--quiet[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'-v[Trace what is scanned and summarize every comment kind and skipped file]' \
'--verbose[Trace what is scanned and summarize every comment kind and skipped file]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':path -- Directory to create the plugin crate in:_files' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_ocomment__subcmd__plugin__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:ocomment-plugin-help-command-$line[1]:"
        case $line[1] in
            (add)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(remove)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(update)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(verify)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(new)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(completions)
_arguments "${_arguments_options[@]}" : \
'--config=[Read this configuration file instead of discovering \`.ocomment.toml\`]:FILE:_files' \
'--policy=[Which classes of comment the run is allowed to remove]:POLICY:((safe\:"Remove ordinary and doc comments; keep preambles and directives"
legal\:"Like safe, and keep licence and copyright comments as well"
all\:"Remove every comment that no keep override protects"))' \
'--layout=[How the bytes left behind by a removed comment are laid out]:LAYOUT:((lines\:"Keep the line structure and separate tokens that would otherwise join"
columns\:"Pad each removed comment so the following columns do not shift"
compact\:"Drop lines that held only a removed comment, and the whitespace it left behind"))' \
'--language=[Force this language instead of detecting it from path and contents]:LANGUAGE:((rust\:"Rust source files"
ocaml\:"OCaml implementation and interface files"
c\:"C source and header files"
cpp\:"C++ source and header files"
go\:"Go source files"
java\:"Java source files, including Unicode escape translation"
javascript\:"JavaScript modules and scripts, including JSX"
typescript\:"TypeScript modules and scripts, including TSX"
python\:"Python source and stub files"
shell\:"POSIX sh, Bash, and zsh scripts"
html\:"HTML documents, including nested script and style elements"
css\:"CSS stylesheets"
jsonc\:"JSON with comments, including JSON5"
sql\:"SQL for every supported database dialect"
kotlin\:"Kotlin source and script files"
toml\:"TOML documents, including the lock files written in it"
lua\:"Lua chunks and LuaRocks rockspecs"
yaml\:"YAML documents, including the tool configurations written in it"
php\:"PHP scripts and templates; the inline HTML around the tags is content"
ruby\:"Ruby scripts, gem manifests, and the project files named after their tool"))' \
'--dialect=[Force this dialect of the selected language]:DIALECT:((standard\:"The default lexical rules of the language"
jsx\:"JavaScript with JSX elements"
tsx\:"TypeScript with JSX elements"
objective-c\:"Objective-C extensions to C"
objective-cpp\:"Objective-C++ extensions to C++"
gnu-c\:"GNU extensions to C"
gnu-cpp\:"GNU extensions to C++"
cuda\:"CUDA extensions to C++"
posix-sh\:"The POSIX shell command language"
bash53\:"Bash 5.3"
zsh\:"The Z shell"
postgresql\:"PostgreSQL, with dollar-quoted bodies"
mysql\:"MySQL, including its executable versioned comments"
sqlite\:"SQLite"
t-sql\:"Microsoft Transact-SQL"
oracle\:"Oracle SQL and PL/SQL"))' \
'*--keep-kind=[Comma-separated comment kinds to protect on top of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'*--remove-kind=[Comma-separated comment kinds to remove regardless of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'--format=[Output encoding]:FORMAT:(human json jsonl sarif github)' \
'--color=[When to colour terminal output]:WHEN:(auto always never)' \
'--hyperlinks=[When to emit terminal hyperlinks for reported paths]:WHEN:(auto always never)' \
'--progress=[When to draw the live scanning counter on standard error]:WHEN:(auto always never)' \
'--force-invalid[Apply the edits that are still provably safe when the source fails to scan]' \
'--force-protected[Remove protected comments such as shebang and encoding preambles]' \
'--no-preview[Omit the one-line comment text from human \`check\` and \`scan\` lines]' \
'--explain[List every comment human \`check\` and \`scan\` met and name the rule and setting behind each one]' \
'(-v --verbose)-q[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'(-v --verbose)--quiet[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'-v[Trace what is scanned and summarize every comment kind and skipped file]' \
'--verbose[Trace what is scanned and summarize every comment kind and skipped file]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':shell -- Shell whose completion script is written to stdout:(bash elvish fish powershell zsh)' \
&& ret=0
;;
(doctor)
_arguments "${_arguments_options[@]}" : \
'--config=[Read this configuration file instead of discovering \`.ocomment.toml\`]:FILE:_files' \
'--policy=[Which classes of comment the run is allowed to remove]:POLICY:((safe\:"Remove ordinary and doc comments; keep preambles and directives"
legal\:"Like safe, and keep licence and copyright comments as well"
all\:"Remove every comment that no keep override protects"))' \
'--layout=[How the bytes left behind by a removed comment are laid out]:LAYOUT:((lines\:"Keep the line structure and separate tokens that would otherwise join"
columns\:"Pad each removed comment so the following columns do not shift"
compact\:"Drop lines that held only a removed comment, and the whitespace it left behind"))' \
'--language=[Force this language instead of detecting it from path and contents]:LANGUAGE:((rust\:"Rust source files"
ocaml\:"OCaml implementation and interface files"
c\:"C source and header files"
cpp\:"C++ source and header files"
go\:"Go source files"
java\:"Java source files, including Unicode escape translation"
javascript\:"JavaScript modules and scripts, including JSX"
typescript\:"TypeScript modules and scripts, including TSX"
python\:"Python source and stub files"
shell\:"POSIX sh, Bash, and zsh scripts"
html\:"HTML documents, including nested script and style elements"
css\:"CSS stylesheets"
jsonc\:"JSON with comments, including JSON5"
sql\:"SQL for every supported database dialect"
kotlin\:"Kotlin source and script files"
toml\:"TOML documents, including the lock files written in it"
lua\:"Lua chunks and LuaRocks rockspecs"
yaml\:"YAML documents, including the tool configurations written in it"
php\:"PHP scripts and templates; the inline HTML around the tags is content"
ruby\:"Ruby scripts, gem manifests, and the project files named after their tool"))' \
'--dialect=[Force this dialect of the selected language]:DIALECT:((standard\:"The default lexical rules of the language"
jsx\:"JavaScript with JSX elements"
tsx\:"TypeScript with JSX elements"
objective-c\:"Objective-C extensions to C"
objective-cpp\:"Objective-C++ extensions to C++"
gnu-c\:"GNU extensions to C"
gnu-cpp\:"GNU extensions to C++"
cuda\:"CUDA extensions to C++"
posix-sh\:"The POSIX shell command language"
bash53\:"Bash 5.3"
zsh\:"The Z shell"
postgresql\:"PostgreSQL, with dollar-quoted bodies"
mysql\:"MySQL, including its executable versioned comments"
sqlite\:"SQLite"
t-sql\:"Microsoft Transact-SQL"
oracle\:"Oracle SQL and PL/SQL"))' \
'*--keep-kind=[Comma-separated comment kinds to protect on top of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'*--remove-kind=[Comma-separated comment kinds to remove regardless of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'--format=[Output encoding]:FORMAT:(human json jsonl sarif github)' \
'--color=[When to colour terminal output]:WHEN:(auto always never)' \
'--hyperlinks=[When to emit terminal hyperlinks for reported paths]:WHEN:(auto always never)' \
'--progress=[When to draw the live scanning counter on standard error]:WHEN:(auto always never)' \
'--force-invalid[Apply the edits that are still provably safe when the source fails to scan]' \
'--force-protected[Remove protected comments such as shebang and encoding preambles]' \
'--no-preview[Omit the one-line comment text from human \`check\` and \`scan\` lines]' \
'--explain[List every comment human \`check\` and \`scan\` met and name the rule and setting behind each one]' \
'(-v --verbose)-q[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'(-v --verbose)--quiet[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'-v[Trace what is scanned and summarize every comment kind and skipped file]' \
'--verbose[Trace what is scanned and summarize every comment kind and skipped file]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(man)
_arguments "${_arguments_options[@]}" : \
'--config=[Read this configuration file instead of discovering \`.ocomment.toml\`]:FILE:_files' \
'--policy=[Which classes of comment the run is allowed to remove]:POLICY:((safe\:"Remove ordinary and doc comments; keep preambles and directives"
legal\:"Like safe, and keep licence and copyright comments as well"
all\:"Remove every comment that no keep override protects"))' \
'--layout=[How the bytes left behind by a removed comment are laid out]:LAYOUT:((lines\:"Keep the line structure and separate tokens that would otherwise join"
columns\:"Pad each removed comment so the following columns do not shift"
compact\:"Drop lines that held only a removed comment, and the whitespace it left behind"))' \
'--language=[Force this language instead of detecting it from path and contents]:LANGUAGE:((rust\:"Rust source files"
ocaml\:"OCaml implementation and interface files"
c\:"C source and header files"
cpp\:"C++ source and header files"
go\:"Go source files"
java\:"Java source files, including Unicode escape translation"
javascript\:"JavaScript modules and scripts, including JSX"
typescript\:"TypeScript modules and scripts, including TSX"
python\:"Python source and stub files"
shell\:"POSIX sh, Bash, and zsh scripts"
html\:"HTML documents, including nested script and style elements"
css\:"CSS stylesheets"
jsonc\:"JSON with comments, including JSON5"
sql\:"SQL for every supported database dialect"
kotlin\:"Kotlin source and script files"
toml\:"TOML documents, including the lock files written in it"
lua\:"Lua chunks and LuaRocks rockspecs"
yaml\:"YAML documents, including the tool configurations written in it"
php\:"PHP scripts and templates; the inline HTML around the tags is content"
ruby\:"Ruby scripts, gem manifests, and the project files named after their tool"))' \
'--dialect=[Force this dialect of the selected language]:DIALECT:((standard\:"The default lexical rules of the language"
jsx\:"JavaScript with JSX elements"
tsx\:"TypeScript with JSX elements"
objective-c\:"Objective-C extensions to C"
objective-cpp\:"Objective-C++ extensions to C++"
gnu-c\:"GNU extensions to C"
gnu-cpp\:"GNU extensions to C++"
cuda\:"CUDA extensions to C++"
posix-sh\:"The POSIX shell command language"
bash53\:"Bash 5.3"
zsh\:"The Z shell"
postgresql\:"PostgreSQL, with dollar-quoted bodies"
mysql\:"MySQL, including its executable versioned comments"
sqlite\:"SQLite"
t-sql\:"Microsoft Transact-SQL"
oracle\:"Oracle SQL and PL/SQL"))' \
'*--keep-kind=[Comma-separated comment kinds to protect on top of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'*--remove-kind=[Comma-separated comment kinds to remove regardless of the policy]:KIND:((line\:"An ordinary comment running to the end of the line"
block\:"An ordinary delimited comment"
doc-line\:"A documentation comment running to the end of the line"
doc-block\:"A delimited documentation comment"
directive\:"A tool or language directive such as a pragma or lint control"
license\:"A licence or copyright preamble"
html-comment\:"A DOM-observable HTML comment"
shebang\:"The interpreter line starting an executable script"
encoding\:"A source encoding declaration"
optimizer-hint\:"A compiler or database optimizer hint"
version-comment\:"A MySQL versioned comment that the server executes"))' \
'--format=[Output encoding]:FORMAT:(human json jsonl sarif github)' \
'--color=[When to colour terminal output]:WHEN:(auto always never)' \
'--hyperlinks=[When to emit terminal hyperlinks for reported paths]:WHEN:(auto always never)' \
'--progress=[When to draw the live scanning counter on standard error]:WHEN:(auto always never)' \
'--force-invalid[Apply the edits that are still provably safe when the source fails to scan]' \
'--force-protected[Remove protected comments such as shebang and encoding preambles]' \
'--no-preview[Omit the one-line comment text from human \`check\` and \`scan\` lines]' \
'--explain[List every comment human \`check\` and \`scan\` met and name the rule and setting behind each one]' \
'(-v --verbose)-q[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'(-v --verbose)--quiet[Drop the run summary and notes; the command'\''s product (findings, patch, listing) is still written]' \
'-v[Trace what is scanned and summarize every comment kind and skipped file]' \
'--verbose[Trace what is scanned and summarize every comment kind and skipped file]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_ocomment__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:ocomment-help-command-$line[1]:"
        case $line[1] in
            (check)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(fix)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(diff)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(scan)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(strip)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(lsp)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(init)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(config)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(languages)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(plugin)
_arguments "${_arguments_options[@]}" : \
":: :_ocomment__subcmd__help__subcmd__plugin_commands" \
"*::: :->plugin" \
&& ret=0

    case $state in
    (plugin)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:ocomment-help-plugin-command-$line[1]:"
        case $line[1] in
            (add)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(remove)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(update)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(verify)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(new)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(completions)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(doctor)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(man)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
}

(( $+functions[_ocomment_commands] )) ||
_ocomment_commands() {
    local commands; commands=(
'check:Report removable comments (default command)' \
'fix:Remove comments in place through an atomic, rollback-backed transaction' \
'diff:Print a unified diff of the changes fix would make' \
'scan:List every comment with its kind, disposition and byte span' \
'strip:Read source on stdin and write the stripped result to stdout' \
'lsp:Run the LSP 3.18 server over stdio' \
'init:Write a starter .ocomment.toml or Lefthook configuration' \
'config:Show, locate, explain, or export the resolved configuration' \
'languages:List built-in languages, extensions, and dialects' \
'plugin:Manage sandboxed WASM scanner plugins' \
'completions:Generate shell completions' \
'doctor:Diagnose the environment (config, git, plugins, tools)' \
'man:Render the roff manual page to stdout' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'ocomment commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__check_commands] )) ||
_ocomment__subcmd__check_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment check commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__completions_commands] )) ||
_ocomment__subcmd__completions_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment completions commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__config_commands] )) ||
_ocomment__subcmd__config_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment config commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__diff_commands] )) ||
_ocomment__subcmd__diff_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment diff commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__doctor_commands] )) ||
_ocomment__subcmd__doctor_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment doctor commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__fix_commands] )) ||
_ocomment__subcmd__fix_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment fix commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__help_commands] )) ||
_ocomment__subcmd__help_commands() {
    local commands; commands=(
'check:Report removable comments (default command)' \
'fix:Remove comments in place through an atomic, rollback-backed transaction' \
'diff:Print a unified diff of the changes fix would make' \
'scan:List every comment with its kind, disposition and byte span' \
'strip:Read source on stdin and write the stripped result to stdout' \
'lsp:Run the LSP 3.18 server over stdio' \
'init:Write a starter .ocomment.toml or Lefthook configuration' \
'config:Show, locate, explain, or export the resolved configuration' \
'languages:List built-in languages, extensions, and dialects' \
'plugin:Manage sandboxed WASM scanner plugins' \
'completions:Generate shell completions' \
'doctor:Diagnose the environment (config, git, plugins, tools)' \
'man:Render the roff manual page to stdout' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'ocomment help commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__help__subcmd__check_commands] )) ||
_ocomment__subcmd__help__subcmd__check_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment help check commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__help__subcmd__completions_commands] )) ||
_ocomment__subcmd__help__subcmd__completions_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment help completions commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__help__subcmd__config_commands] )) ||
_ocomment__subcmd__help__subcmd__config_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment help config commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__help__subcmd__diff_commands] )) ||
_ocomment__subcmd__help__subcmd__diff_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment help diff commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__help__subcmd__doctor_commands] )) ||
_ocomment__subcmd__help__subcmd__doctor_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment help doctor commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__help__subcmd__fix_commands] )) ||
_ocomment__subcmd__help__subcmd__fix_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment help fix commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__help__subcmd__help_commands] )) ||
_ocomment__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment help help commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__help__subcmd__init_commands] )) ||
_ocomment__subcmd__help__subcmd__init_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment help init commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__help__subcmd__languages_commands] )) ||
_ocomment__subcmd__help__subcmd__languages_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment help languages commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__help__subcmd__lsp_commands] )) ||
_ocomment__subcmd__help__subcmd__lsp_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment help lsp commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__help__subcmd__man_commands] )) ||
_ocomment__subcmd__help__subcmd__man_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment help man commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__help__subcmd__plugin_commands] )) ||
_ocomment__subcmd__help__subcmd__plugin_commands() {
    local commands; commands=(
'add:Install a plugin and pin its digest in .ocomment.lock' \
'remove:Uninstall a plugin and drop its lock entry' \
'list:List the installed plugins and their pinned digests' \
'update:Re-fetch plugins and refresh their pinned digests' \
'verify:Check installed plugins against their pinned digests' \
'new:Scaffold a new plugin crate from the scanner WIT world' \
    )
    _describe -t commands 'ocomment help plugin commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__help__subcmd__plugin__subcmd__add_commands] )) ||
_ocomment__subcmd__help__subcmd__plugin__subcmd__add_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment help plugin add commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__help__subcmd__plugin__subcmd__list_commands] )) ||
_ocomment__subcmd__help__subcmd__plugin__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment help plugin list commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__help__subcmd__plugin__subcmd__new_commands] )) ||
_ocomment__subcmd__help__subcmd__plugin__subcmd__new_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment help plugin new commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__help__subcmd__plugin__subcmd__remove_commands] )) ||
_ocomment__subcmd__help__subcmd__plugin__subcmd__remove_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment help plugin remove commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__help__subcmd__plugin__subcmd__update_commands] )) ||
_ocomment__subcmd__help__subcmd__plugin__subcmd__update_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment help plugin update commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__help__subcmd__plugin__subcmd__verify_commands] )) ||
_ocomment__subcmd__help__subcmd__plugin__subcmd__verify_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment help plugin verify commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__help__subcmd__scan_commands] )) ||
_ocomment__subcmd__help__subcmd__scan_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment help scan commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__help__subcmd__strip_commands] )) ||
_ocomment__subcmd__help__subcmd__strip_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment help strip commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__init_commands] )) ||
_ocomment__subcmd__init_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment init commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__languages_commands] )) ||
_ocomment__subcmd__languages_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment languages commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__lsp_commands] )) ||
_ocomment__subcmd__lsp_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment lsp commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__man_commands] )) ||
_ocomment__subcmd__man_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment man commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__plugin_commands] )) ||
_ocomment__subcmd__plugin_commands() {
    local commands; commands=(
'add:Install a plugin and pin its digest in .ocomment.lock' \
'remove:Uninstall a plugin and drop its lock entry' \
'list:List the installed plugins and their pinned digests' \
'update:Re-fetch plugins and refresh their pinned digests' \
'verify:Check installed plugins against their pinned digests' \
'new:Scaffold a new plugin crate from the scanner WIT world' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'ocomment plugin commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__plugin__subcmd__add_commands] )) ||
_ocomment__subcmd__plugin__subcmd__add_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment plugin add commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__plugin__subcmd__help_commands] )) ||
_ocomment__subcmd__plugin__subcmd__help_commands() {
    local commands; commands=(
'add:Install a plugin and pin its digest in .ocomment.lock' \
'remove:Uninstall a plugin and drop its lock entry' \
'list:List the installed plugins and their pinned digests' \
'update:Re-fetch plugins and refresh their pinned digests' \
'verify:Check installed plugins against their pinned digests' \
'new:Scaffold a new plugin crate from the scanner WIT world' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'ocomment plugin help commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__plugin__subcmd__help__subcmd__add_commands] )) ||
_ocomment__subcmd__plugin__subcmd__help__subcmd__add_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment plugin help add commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__plugin__subcmd__help__subcmd__help_commands] )) ||
_ocomment__subcmd__plugin__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment plugin help help commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__plugin__subcmd__help__subcmd__list_commands] )) ||
_ocomment__subcmd__plugin__subcmd__help__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment plugin help list commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__plugin__subcmd__help__subcmd__new_commands] )) ||
_ocomment__subcmd__plugin__subcmd__help__subcmd__new_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment plugin help new commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__plugin__subcmd__help__subcmd__remove_commands] )) ||
_ocomment__subcmd__plugin__subcmd__help__subcmd__remove_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment plugin help remove commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__plugin__subcmd__help__subcmd__update_commands] )) ||
_ocomment__subcmd__plugin__subcmd__help__subcmd__update_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment plugin help update commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__plugin__subcmd__help__subcmd__verify_commands] )) ||
_ocomment__subcmd__plugin__subcmd__help__subcmd__verify_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment plugin help verify commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__plugin__subcmd__list_commands] )) ||
_ocomment__subcmd__plugin__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment plugin list commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__plugin__subcmd__new_commands] )) ||
_ocomment__subcmd__plugin__subcmd__new_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment plugin new commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__plugin__subcmd__remove_commands] )) ||
_ocomment__subcmd__plugin__subcmd__remove_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment plugin remove commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__plugin__subcmd__update_commands] )) ||
_ocomment__subcmd__plugin__subcmd__update_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment plugin update commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__plugin__subcmd__verify_commands] )) ||
_ocomment__subcmd__plugin__subcmd__verify_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment plugin verify commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__scan_commands] )) ||
_ocomment__subcmd__scan_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment scan commands' commands "$@"
}
(( $+functions[_ocomment__subcmd__strip_commands] )) ||
_ocomment__subcmd__strip_commands() {
    local commands; commands=()
    _describe -t commands 'ocomment strip commands' commands "$@"
}

if [ "$funcstack[1]" = "_ocomment" ]; then
    _ocomment "$@"
else
    compdef _ocomment ocomment
fi
